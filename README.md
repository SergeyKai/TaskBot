# Конфигурации проекта для корректного запуска:
Токен бота и настройки БД вынесены в переменные окружения. Для запуска необходимо создать файл .env с переменными:
TOKEN
HOST
PORT
USER
PASSWORD
DATABASE
ECHO
